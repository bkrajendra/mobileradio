import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rajendrakhope.mobileradio',
  appName: 'MobileRadio',
  webDir: 'www'
};

export default config;
